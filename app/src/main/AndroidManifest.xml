<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebaseeva">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BildungActivity"></activity>
        <activity android:name=".SpielActivity" />
        <activity android:name=".MusikActivity" />
        <activity android:name=".BewegungActivity" />
        <activity android:name=".AktivitaetKategorie" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".Aktivitaet" />
        <activity android:name=".KlubActivity" />
        <activity android:name=".Interessen" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AnmeldeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>